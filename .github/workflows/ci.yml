name: CI

on:
  push:
  pull_request:

jobs:
  rust:
    name: Rust
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Run clippy
        run: cargo clippy -p agentflow-core -p agentflow-cli -p agentflow-schemas --all-targets -- -D warnings

      - name: Run tests
        run: cargo test -p agentflow-core -p agentflow-cli -p agentflow-schemas

  desktop-rust:
    name: Desktop (Rust)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Install Tauri Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Run clippy
        run: cargo clippy -p agentflow-desktop-api -p agentflow-desktop --all-targets -- -D warnings

      - name: Run tests
        run: cargo test -p agentflow-desktop-api -p agentflow-desktop

  desktop-frontend:
    name: Desktop frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: crates/agentflow-desktop/package-lock.json

      - name: Install dependencies
        working-directory: crates/agentflow-desktop
        run: npm ci

      - name: Type-check and build
        working-directory: crates/agentflow-desktop
        run: npm run build
